import Foundation

enum UserDefaultsKeys {
    static let colorsRestored = "ct.stats.colorsRestored"
    static let starsEarned    = "ct.stats.starsEarned"
    static let levelProgress  = "ct.progress.levels"      // JSON [LevelProgress]
    static let hasFullGame    = "ct.iap.fullGame"
    
    static let musicEnabled     = "ct.settings.music"
    static let soundEnabled     = "ct.settings.sound"
    static let vibrationEnabled = "ct.settings.vibration"
}
